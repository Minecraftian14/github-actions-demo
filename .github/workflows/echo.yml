name: Demo

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello from GitHub Actions!"

      - name: Show commit
        run: |
          echo "Commit: ${{ github.sha }}"